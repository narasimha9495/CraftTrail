
.lp__hero {
  display: grid; grid-template-columns: 1fr 1fr; gap: 48px;
  padding: 64px 24px 60px; align-items: center;
}
.lp__copy h1 { margin-top: 14px; max-width: 15ch; }
.lp__copy h1 .script { font-size: 1.05em; }
.lp__sub { margin: 22px 0 0; max-width: 50ch; font-size: 1.02rem; color: var(--ink-mid); }
.lp__cta { display: flex; gap: 10px; margin-top: 28px; flex-wrap: wrap; }
.lp__cta .btn { text-decoration: none; }
.lp__fine { margin: 14px 0 0; font-size: .76rem; color: var(--ink-dim); max-width: 44ch; }

.lp__loom {
  padding: 0; overflow: hidden; height: 460px; display: flex; flex-direction: column;
  align-items: center; justify-content: center; background: var(--surface);
}
.lp__loom svg {
  width: 100%; max-width: 340px; height: auto; flex: 1;
  padding: 28px 20px 0;
}
.lp__loom .lp__maphint { width: 100%; }

.lp__map { padding: 0; overflow: hidden; display: flex; flex-direction: column; }
.lp__map .map { flex: 1; border-radius: 0; }
.lp__maphint {
  margin: 0; padding: 10px 14px; font-size: .68rem;
  color: var(--ink-dim); border-top: 1px solid var(--line); background: var(--surface);
}

.lp__facts { background: var(--sunk); border-top: 1px solid var(--line); border-bottom: 1px solid var(--line); padding: 44px 0 38px; }
.lp__factrow { display: grid; grid-template-columns: repeat(4, 1fr); gap: 26px; }
.fact { display: flex; flex-direction: column; gap: 5px; }
.fact__v {
  font-family: var(--display); font-weight: 900; font-size: clamp(1.6rem, 3vw, 2.4rem);
  letter-spacing: -.03em; line-height: 1;
}
.fact:last-child .fact__v { color: var(--accent); }
.fact__l { font-size: .78rem; color: var(--ink-mid); line-height: 1.4; }
.lp__factnote { margin: 30px auto 0; font-size: .82rem; color: var(--ink-dim); max-width: 62ch; }

/* Map section — now full width */
.lp__mapsection { padding: 70px 0; text-align: center; }
.lp__mapsectionHead h2 { margin-top: 10px; }
.lp__mapsectionHead .lp__lede { margin: 16px auto 0; max-width: 58ch; }

.lp__map--full {
  width: 360px;
  max-width: 100%;
  margin: 36px auto 0;
  height: 480px;
  text-align: left;
  border-radius: var(--r-lg, 16px);
  overflow: hidden;
  box-shadow: 0 12px 40px rgba(0,0,0,0.14);
}
.lp__map--full .map { border-radius: 0; }

/* Shop by state — auto-scrolling carousel */
.lp__states { padding: 0 0 70px; overflow: hidden; }
.lp__statesHead { text-align: center; padding-bottom: 8px; }
.lp__statesHead h2 { margin-top: 10px; }
.lp__statesHead .lp__lede { margin: 16px auto 0; max-width: 58ch; }

.states-viewport {
  overflow-x: hidden;
  overflow-y: visible;
  padding: 40px 0 20px;
  mask-image: linear-gradient(to right, transparent 0, black 40px, black calc(100% - 40px), transparent 100%);
  -webkit-mask-image: linear-gradient(to right, transparent 0, black 40px, black calc(100% - 40px), transparent 100%);
}

.states-track {
  display: flex;
  gap: 20px;
  width: max-content;
  animation: states-scroll 38s linear infinite;
  padding: 0 24px;
}
.states-track:hover { animation-play-state: paused; }

@keyframes states-scroll {
  from { transform: translateX(0); }
  to { transform: translateX(-50%); }
}

.state-card {
  position: relative;
  flex: 0 0 220px;
  height: 260px;
  border-radius: var(--r-lg);
  border: 1px solid var(--line);
  overflow: visible;
  cursor: pointer;
  transition: transform .25s ease, box-shadow .25s ease;
  background: var(--surface);
}
.state-card:hover {
  transform: translateY(-6px) scale(1.04);
  box-shadow: 0 18px 40px rgba(20, 20, 30, 0.18);
  z-index: 5;
}

/* FIXED SECTION: Stays neatly clipped inside rounded corners & renders underneath tooltips */
.state-card__pattern {
  position: absolute; 
  inset: 0;
  border-radius: var(--r-lg);
  overflow: hidden;
  z-index: 1;
  transform: translateZ(0);
  -webkit-mask-image: -webkit-radial-gradient(white, black);
}
.state-card__pattern::before { 
  content: ""; 
  position: absolute; 
  inset: 0; 
  width: 100%;
  height: 100%;
  opacity: .9; 
}

/* Telangana */
.swatch--ikat .state-card__pattern::before{
    background:url("/telangana.jpg") center/cover no-repeat;
}

/* Andhra Pradesh */
.swatch--kalamkari .state-card__pattern::before{
    background:url("/andhra.jpg") center/cover no-repeat;
}

/* Rajasthan */
.swatch--blockprint .state-card__pattern::before{
    background:url("/rajasthan.jpg") center/cover no-repeat;
}

/* Gujarat */
.swatch--bandhani .state-card__pattern::before{
    background:url("/gujarat.jpg") center/cover no-repeat;
}

/* Karnataka */
.swatch--silk .state-card__pattern::before{
    background:url("/karnataka.jpg") center/cover no-repeat;
}

/* Tamil Nadu */
.swatch--bronze .state-card__pattern::before{
    background:url("/tn.jpg") center/cover no-repeat;
}

/* West Bengal */
.swatch--kantha .state-card__pattern::before{
    background:url("/westbengal.jpg") center/cover no-repeat;
}

/* Odisha */
.swatch--pattachitra .state-card__pattern::before{
    background:url("/odisha.jpg") center/cover no-repeat;
}

/* Uttar Pradesh */
.swatch--chikankari .state-card__pattern::before{
    background:url("/up.jpg") center/cover no-repeat;
}

/* Madhya Pradesh */
.swatch--gond .state-card__pattern::before{
    background:url("/mp.jpg") center/cover no-repeat;
}

/* Bihar */
.swatch--madhubani .state-card__pattern::before{
    background:url("/bihar.jpg") center/cover no-repeat;
}

/* Assam */
.swatch--muga .state-card__pattern::before{
    background:url("/assam.jpg") center/cover no-repeat;
}

/* Kerala */
.swatch--coir .state-card__pattern::before{
    background:url("/kerala.jpg") center/cover no-repeat;
}

/* Jammu & Kashmir */
.swatch--pashmina .state-card__pattern::before{
    background:url("/kashmir.jpg") center/cover no-repeat;
}

/* Punjab */
.swatch--phulkari .state-card__pattern::before{
    background:url("/punjab.jpg") center/cover no-repeat;
}

/* Himachal Pradesh */
.swatch--kullu .state-card__pattern::before{
    background:url("/himachal.jpg") center/cover no-repeat;
}

/* FIXED SECTION: Stays pinned on top of the background image safely */
.state-card__label {
  position: absolute; left: 0; right: 0; bottom: 0;
  padding: 14px 16px;
  background: linear-gradient(to top, rgba(20,16,10,.82), rgba(20,16,10,0));
  color: #fff;
  border-radius: 0 0 var(--r-lg) var(--r-lg);
  display: flex; flex-direction: column; gap: 3px;
  z-index: 2;
}
.state-card__name { font-family: var(--display); font-weight: 800; font-size: 1.05rem; }
.state-card__craft { font-size: .72rem; opacity: .85; }

.state-card__detail {
  position: absolute;
  left: 50%;
  bottom: calc(100% + 12px);
  transform: translate(-50%, 8px);
  width: 260px;
  background: var(--ink, #1f2a44);
  color: #fff;
  border-radius: var(--r-lg);
  padding: 16px 18px;
  font-size: .8rem;
  line-height: 1.5;
  opacity: 0;
  pointer-events: none;
  transition: opacity .2s ease, transform .2s ease;
  box-shadow: 0 20px 40px rgba(0,0,0,.25);
  z-index: 10;
}
.state-card__detail::after {
  content: "";
  position: absolute;
  top: 100%; left: 50%;
  transform: translateX(-50%);
  border: 7px solid transparent;
  border-top-color: var(--ink, #1f2a44);
}
.state-card.is-active .state-card__detail {
  opacity: 1;
  pointer-events: auto;
  transform: translate(-50%, 0);
}
.state-card__detail p { margin: 0 0 10px; }
.state-card__link {
  color: #f4ede0;
  font-size: .74rem;
  text-decoration: none;
  border-bottom: 1px solid rgba(255,255,255,.4);
}
.state-card__link:hover { border-color: #fff; }

.lp__trust { padding: 70px 24px 20px; }
.lp__trust h2 { margin-top: 10px; }
.lp__lede { margin: 16px 0 0; max-width: 58ch; color: var(--ink-mid); }

.tiers { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; margin-top: 36px; }
.tier {
  border: 1px solid var(--line); border-radius: var(--r-lg);
  padding: 22px; background: var(--surface); position: relative;
}
.tier__cap {
  font-size: 2.1rem; font-weight: 500; color: var(--accent-2); line-height: 1;
  display: block; margin-bottom: 12px;
}
.tier:nth-child(2) .tier__cap { color: var(--accent); }
.tier:nth-child(3) .tier__cap { color: var(--live); }
.tier__detail { margin: 8px 0 0; font-size: .85rem; color: var(--ink-mid); line-height: 1.55; }
.tier__ceiling {
  margin: 16px 0 0; padding-top: 12px; border-top: 1px solid var(--line);
  font-size: .68rem; color: var(--ink-dim);
}

.lp__crafts { padding: 70px 24px 0; }
.lp__crafts h2 { margin-top: 10px; }
.chips { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 24px; }
.chip {
  border: 1px solid var(--line); border-radius: 99px; padding: 7px 15px;
  font-size: .82rem; color: var(--ink-mid); text-decoration: none; background: var(--surface);
}
.chip:hover { border-color: var(--accent); color: var(--ink); }

.lp__artisans { padding: 70px 24px 80px; }
.artcard {
  display: grid; grid-template-columns: 1.3fr 1fr; gap: 40px; padding: 38px;
}
.artcard h2 { margin-top: 8px; }
.artcard__facts { display: flex; flex-direction: column; gap: 20px; justify-content: center; }
.artcard__facts div { display: flex; flex-direction: column; }
.artcard__facts strong {
  font-family: var(--display); font-size: 1.9rem; font-weight: 900;
  letter-spacing: -.02em; line-height: 1;
}
.artcard__facts span { font-size: .78rem; color: var(--ink-dim); margin-top: 4px; }

.lp__foot { border-top: 1px solid var(--line); padding: 30px 0 44px; background: var(--sunk); }
.lp__footin { display: flex; justify-content: space-between; align-items: center; gap: 20px; flex-wrap: wrap; }
.lp__brand { margin: 0; font-family: var(--display); font-weight: 900; font-size: 1.05rem; }
.lp__tagline { margin: 2px 0 0; font-size: .76rem; color: var(--ink-dim); }
.lp__links { display: flex; gap: 18px; font-size: .82rem; }
.lp__links a { color: var(--ink-dim); text-decoration: none; }
.lp__links a:hover { color: var(--ink); }

@media (max-width: 980px) {
  .lp__hero { grid-template-columns: 1fr; gap: 32px; padding-top: 40px; }
  .lp__loom { height: 320px; }
  .lp__map--full { height: 360px; padding-left: 12px; padding-right: 12px; }
  .lp__factrow { grid-template-columns: 1fr 1fr; }
  .tiers { grid-template-columns: 1fr; }
  .artcard { grid-template-columns: 1fr; gap: 26px; padding: 26px; }
  .state-card { flex-basis: 180px; height: 220px; }
  .state-card__detail { width: 210px; }
}

@media (prefers-reduced-motion: reduce) {
  .states-track { animation: none; }
}

/* ── State card login hint ────────────────────────────────────────── */
.state-card__login-hint {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background: rgba(0, 0, 0, 0.65);
  color: #fff;
  font-size: 0.68rem;
  font-weight: 600;
  letter-spacing: 0.04em;
  padding: 5px 10px;
  border-radius: 99px;
  white-space: nowrap;
  opacity: 0;
  transition: opacity 0.2s ease;
  pointer-events: none;
  z-index: 3;
}
.state-card:hover .state-card__login-hint { opacity: 1; }

/* AI Guide badge — shown on state cards when user is logged in */
.state-card__ai-hint {
  position: absolute;
  top: 10px;
  right: 10px;
  background: rgba(192, 73, 46, 0.9);
  color: #fff;
  font-size: 0.6rem;
  font-weight: 700;
  letter-spacing: 0.05em;
  padding: 3px 8px;
  border-radius: 99px;
  white-space: nowrap;
  opacity: 0;
  transition: opacity 0.2s ease, transform 0.2s ease;
  transform: translateY(-4px);
  pointer-events: none;
  z-index: 3;
  backdrop-filter: blur(4px);
}
.state-card.is-unlocked:hover .state-card__ai-hint {
  opacity: 1;
  transform: translateY(0);
}
/* Subtle glow on unlocked cards to signal interactivity */
.state-card.is-unlocked:hover {
  box-shadow: 0 18px 40px rgba(192, 73, 46, 0.28), 0 0 0 2px rgba(192, 73, 46, 0.3);
}

/* Unlocked badge above state carousel */
.lp__states-unlocked {
  margin: 16px auto 0;
  text-align: center;
}
.lp__unlock-badge {
  display: inline-flex;
  align-items: center;
  gap: 7px;
  background: linear-gradient(135deg, rgba(192, 73, 46, 0.1), rgba(192, 73, 46, 0.05));
  border: 1px solid rgba(192, 73, 46, 0.3);
  color: var(--ink-mid, #555);
  font-size: 0.8rem;
  font-weight: 500;
  padding: 8px 18px;
  border-radius: 99px;
  animation: unlock-pulse 3s ease-in-out infinite;
}
@keyframes unlock-pulse {
  0%, 100% { box-shadow: 0 0 0 0 rgba(192, 73, 46, 0.15); }
  50%       { box-shadow: 0 0 0 8px rgba(192, 73, 46, 0); }
}

/* Welcome fine text for logged-in users */
.lp__fine--welcome {
  color: var(--accent, #c0492e) !important;
  font-weight: 500 !important;
  font-size: 0.8rem !important;
}


/* ── Login prompt modal ───────────────────────────────────────────── */
.lpm__overlay {
  position: fixed;
  inset: 0;
  z-index: 1100;
  background: rgba(10, 10, 20, 0.6);
  backdrop-filter: blur(4px);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 20px;
  animation: lpm-fade 0.2s ease;
}
@keyframes lpm-fade { from { opacity: 0; } }

.lpm__box {
  position: relative;
  background: var(--surface, #fff);
  border-radius: 20px;
  padding: 36px 32px 30px;
  max-width: 400px;
  width: 100%;
  text-align: center;
  box-shadow: 0 24px 60px rgba(0,0,0,0.3);
  animation: lpm-slide 0.25s cubic-bezier(0.22, 1, 0.36, 1);
}
@keyframes lpm-slide {
  from { opacity: 0; transform: scale(0.93) translateY(16px); }
}

.lpm__close {
  position: absolute;
  top: 12px;
  right: 14px;
  background: var(--ground, #f8f6f2);
  border: 1px solid var(--line, #e0e0e0);
  border-radius: 50%;
  width: 32px;
  height: 32px;
  font-size: 1.2rem;
  cursor: pointer;
  display: grid;
  place-items: center;
  color: var(--ink-dim, #888);
  transition: background 0.15s;
}
.lpm__close:hover { background: var(--line, #e0e0e0); }

.lpm__icon {
  font-size: 2.6rem;
  margin-bottom: 14px;
}
.lpm__box h3 {
  font-family: var(--display, serif);
  font-size: 1.3rem;
  font-weight: 800;
  margin: 0 0 10px;
  line-height: 1.3;
}
.lpm__box p {
  font-size: 0.84rem;
  color: var(--ink-mid, #555);
  line-height: 1.6;
  margin: 0 0 22px;
}
.lpm__actions {
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.lpm__wide {
  width: 100%;
  text-decoration: none;
  display: block;
  text-align: center;
}
.lpm__fine {
  margin: 14px 0 0 !important;
  font-size: 0.7rem !important;
  color: var(--ink-dim, #aaa) !important;
}